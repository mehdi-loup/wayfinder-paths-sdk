# Examples

Useful commands:
- `wayfinder path doctor --path examples/paths/quant-desk-pipeline`
- `wayfinder path eval --path examples/paths/quant-desk-pipeline`
- `wayfinder path render-skill --path examples/paths/quant-desk-pipeline`

Use the fixtures to validate output shape (PASS → `armed`), null-state selection (zero reproduction → `null`), regime-gate behavior (REGIME_DEPENDENT → `draft`), and host render coverage.
