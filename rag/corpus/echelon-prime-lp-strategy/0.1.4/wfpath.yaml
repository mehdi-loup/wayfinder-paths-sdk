schema_version: '0.1'
slug: echelon-prime-lp-strategy
name: Echelon Prime LP Strategy
version: 0.1.4
summary: Manages PRIME/WETH Uniswap V3 1% LP positions for the Echelon Prime LP Rewards
  Program. Monitors position health, validates compliance (range width, ratio), and
  can open new qualifying positions.
primary_kind: strategy
tags:
- prime
- uniswap
- liquidity
- echelon
- ethereum
- lp-rewards
components:
- id: main
  kind: script
  path: scripts/main.py
skill:
  enabled: true
  source: generated
  name: echelon-prime-lp-strategy
  description: 'Echelon Prime LP Strategy: open and monitor PRIME/WETH Uniswap V3
    1% positions that qualify for Echelon LP rewards.'
  instructions: skill/instructions.md
  runtime:
    mode: thin
    package: wayfinder-paths
    version: 0.10.0
    python: '>=3.12,<3.13'
    component: main
    bootstrap: uv
    fallback_bootstrap: pipx
    prefer_existing_runtime: true
    require_api_key: false
    api_key_env: WAYFINDER_API_KEY
    config_path_env: WAYFINDER_CONFIG_PATH
inputs:
  slots:
    config:
      type: yaml
      path: inputs/config.yaml
      required: true
      description: Wallet label, position range parameters, and Echelon owner wallet
        for dashboard lookups.
